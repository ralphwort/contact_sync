<%= form_for(@fnbl_pim_contact) do |f| %>
  <% if @fnbl_pim_contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fnbl_pim_contact.errors.count, "error") %> prohibited this fnbl_pim_contact from being saved:</h2>

      <ul>
      <% @fnbl_pim_contact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><br />
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %><br />
    <%= f.text_field :last_name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
